{
  "name": "city-drive",
  "version": "1.0.0",
  "description": "Multiplayer city freeroam driving game - static hosting ready",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "terser": "^5.49.0",
    "vite": "^5.3.0"
  }
}
